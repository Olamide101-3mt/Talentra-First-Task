import React from "react";
import { createRoot } from "react-dom/client";
import { BrowserRouter } from "react-router-dom";
import App from "./App";
import "./index.css";
import useAuthStore from "./store/useAuthStore";

// NOTE: authentication will be initialized inside App (fetchCurrentUser) to avoid
// triggering navigation/reload before the router and React tree are ready.

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <BrowserRouter>
      <App />
    </BrowserRouter>
  </React.StrictMode>
);
